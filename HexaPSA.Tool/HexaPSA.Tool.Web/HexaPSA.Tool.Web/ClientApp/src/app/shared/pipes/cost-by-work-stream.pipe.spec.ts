import { CostByWorkStreamPipe } from './cost-by-work-stream.pipe';

describe('CostByWorkStreamPipe', () => {
  it('create an instance', () => {
    const pipe = new CostByWorkStreamPipe();
    expect(pipe).toBeTruthy();
  });
});
